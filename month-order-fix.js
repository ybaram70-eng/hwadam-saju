(()=>{const d=document;let busy=false;function monthNum(el){const t=(el.textContent||'').trim();const m=t.match(/(?:^|\s)(1[0-2]|[1-9])(?:월)?(?:\s|$)/);return m?+m[1]:0}function sortBox(box){if(!box||busy)return;const kids=[...box.children],cards=kids.filter(x=>{const n=monthNum(x);return n>=1&&n<=12});if(cards.length<2)return;const nums=cards.map(monthNum);const needs=nums.some((n,i)=>i&&nums[i-1]>n);if(!needs)return;busy=true;cards.sort((a,b)=>monthNum(a)-monthNum(b)).forEach(x=>box.appendChild(x));busy=false}function scan(){['monthly','hwadamMonthly','monthLuck','monthCards'].forEach(id=>sortBox(d.getElementById(id)));d.querySelectorAll('.monthly,.monthLuck,.monthCards,[data-month-list]').forEach(sortBox)}function boot(){scan();let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(scan,80)}).observe(d.body,{subtree:true,childList:true});setInterval(scan,1200)}if(d.readyState==='loading')d.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();