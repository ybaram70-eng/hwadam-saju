(()=>{const d=document;let busy=false;function monthNum(el){const t=(el.textContent||'').trim();const m=t.match(/^(1[0-2]|[1-9])(?:월)?/);return m?+m[1]:0}function isMonthBox(box){if(!box)return false;const cards=[...box.children].filter(x=>{const n=monthNum(x);return n>=1&&n<=12});return cards.length>=6}function sortBox(box){if(!isMonthBox(box)||busy)return;const cards=[...box.children].filter(x=>monthNum(x));busy=true;cards.sort((a,b)=>monthNum(a)-monthNum(b)).forEach(x=>box.appendChild(x));try{box.scrollLeft=0;box.scrollTo?.({left:0,behavior:'auto'})}catch{}busy=false}function scan(){d.querySelectorAll('.months,.monthly,.monthLuck,.monthCards,[data-month-list],#monthly,#hwadamMonthly,#monthLuck,#monthCards').forEach(sortBox);d.querySelectorAll('.yb,.lb').forEach(card=>{const p=card.parentElement;if(isMonthBox(p))sortBox(p)})}function boot(){scan();let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(scan,100)}).observe(d.body,{subtree:true,childList:true});setInterval(scan,1000)}if(d.readyState==='loading')d.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();