(()=>{const d=document;function init(){const cal=d.getElementById('cfCalendar'),form=d.getElementById('customerForm'),save=d.getElementById('cfSave');if(!cal||!form||!save){return setTimeout(init,150)}if(d.getElementById('cfLeapMonth'))return;const wrap=d.createElement('div');wrap.id='cfLeapWrap';wrap.style.marginTop='6px';wrap.hidden=true;wrap.innerHTML='<select id="cfLeapMonth" style="width:100%;padding:11px;border:1px solid #d7cdbb;border-radius:7px;box-sizing:border-box"><option value="normal">평달</option><option value="leap">윤달</option></select>';cal.closest('.cfGrid2')?.insertAdjacentElement('afterend',wrap);const leap=d.getElementById('cfLeapMonth');const sync=()=>{wrap.hidden=cal.value!=='lunar';if(wrap.hidden)leap.value='normal'};cal.addEventListener('change',sync);const oldOpenAdd=d.getElementById('customerAdd');oldOpenAdd?.addEventListener('click',()=>setTimeout(()=>{leap.value='normal';sync()},0));const oldEdit=d.getElementById('customerEdit');oldEdit?.addEventListener('click',()=>setTimeout(()=>{const key=d.getElementById('journalCustomer')?.value||'';let cs=[];try{cs=JSON.parse(localStorage.getItem('hwadam_customers_v1'))||[]}catch{}const c=cs.find(x=>[x.name||'',x.year||'',x.month||'',x.day||''].join('|')===key);leap.value=c?.isLeapMonth?'leap':'normal';sync()},0));save.addEventListener('click',()=>{if(cal.value!=='lunar')return;setTimeout(()=>{let cs=[];try{cs=JSON.parse(localStorage.getItem('hwadam_customers_v1'))||[]}catch{return}const name=d.getElementById('cfName')?.value.trim()||'',year=d.getElementById('cfYear')?.value||'',month=d.getElementById('cfMonth')?.value||'',day=d.getElementById('cfDay')?.value||'';const c=cs.find(x=>String(x.name)===name&&String(x.year)===String(year)&&String(x.month)===String(month)&&String(x.day)===String(day));if(c){c.isLunar=true;c.isLeapMonth=leap.value==='leap';localStorage.setItem('hwadam_customers_v1',JSON.stringify(cs));const btn=d.getElementById('journalCustomerButton');if(btn)btn.click(),setTimeout(()=>btn.click(),20)}},30)},true);sync()}init()})();