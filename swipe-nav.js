(()=>{const frame=document.getElementById('app');if(!frame)return;const order=['home','input','analysis','calendar','journal'];function activeIndex(){const b=document.querySelector('.navItem.active');const t=b?.dataset?.target;const i=order.indexOf(t);return i<0?0:i}function go(delta){const i=activeIndex(),n=Math.max(0,Math.min(order.length-1,i+delta));if(n===i)return;document.querySelector(`.navItem[data-target="${order[n]}"]`)?.click()}function bind(){let doc;try{doc=frame.contentDocument}catch{return}if(!doc||doc.documentElement.dataset.hwadamSwipeBound)return;doc.documentElement.dataset.hwadamSwipeBound='1';let sx=0,sy=0,started=false,horizontal=false;doc.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;const t=e.target;if(t?.closest?.('input,select,textarea,button,a,.luck,.years,.days,.customerList,#customerButtons,[data-no-swipe]')){started=false;return}sx=e.touches[0].clientX;sy=e.touches[0].clientY;started=true;horizontal=false},{passive:true});doc.addEventListener('touchmove',e=>{if(!started||!e.touches?.length)return;const dx=e.touches[0].clientX-sx,dy=e.touches[0].clientY-sy;if(!horizontal&&Math.abs(dx)>18&&Math.abs(dx)>Math.abs(dy)*1.15)horizontal=true;if(horizontal)e.preventDefault()},{passive:false});doc.addEventListener('touchend',e=>{if(!started||!e.changedTouches?.length)return;started=false;const dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;if(Math.abs(dx)<60||Math.abs(dx)<Math.abs(dy)*1.2)return;if(dx<0)go(1);else go(-1)},{passive:true})}frame.addEventListener('load',()=>setTimeout(bind,100));setTimeout(bind,250)})();