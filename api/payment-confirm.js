export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST 요청만 지원합니다.'});const secret=process.env.TOSS_SECRET_KEY||'',expected=Number(process.env.HWADAM_REPORT_PRICE||0);if(!secret||!expected)return res.status(503).json({error:'결제 서버 설정이 아직 완료되지 않았습니다.'});try{const {paymentKey,orderId,amount}=req.body||{};if(!paymentKey||!orderId||!amount)return res.status(400).json({error:'결제 승인 정보가 부족합니다.'});if(Number(amount)!==expected)return res.status(400).json({error:'결제 금액이 일치하지 않습니다.'});const auth=Buffer.from(secret+':').toString('base64');const r=await fetch('https://api.tosspayments.com/v1/payments/confirm',{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Basic '+auth,'Idempotency-Key':orderId},body:JSON.stringify({paymentKey,orderId,amount:expected})});const data=await r.json();if(!r.ok)return res.status(r.status).json({error:data?.message||'결제 승인에 실패했습니다.',code:data?.code||''});return res.status(200).json({ok:true,paymentKey:data.paymentKey,orderId:data.orderId,status:data.status,totalAmount:data.totalAmount,approvedAt:data.approvedAt,method:data.method})}catch(e){return res.status(500).json({error:e?.message||String(e)})}}