(()=>{const d=document,$=id=>d.getElementById(id);function norm(s){return String(s||'').replace(/\s|·|\./g,'')}function label(box,name){return[...box.querySelectorAll('.hdLabel')].find(x=>norm(x.textContent)===norm(name))}function rowNodes(l){const a=[];let n=l;for(let i=0;i<5&&n;i++,n=n.nextElementSibling)a.push(n);return a}function moveAfter(row,ref){const nodes=rowNodes(row),refNodes=rowNodes(ref),last=refNodes[refNodes.length-1];if(!last||!nodes.length)return;let cur=last;for(const n of nodes){cur.after(n);cur=n}}function apply(){const box=$('hwadamPillarMatrix');if(!box)return;const branch=label(box,'지지십성'),hidden=label(box,'지장간'),hiddenGod=label(box,'지장간십성');if(!branch||!hidden||!hiddenGod)return;moveAfter(hiddenGod,branch);moveAfter(hidden,hiddenGod)}function boot(){apply();let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(apply,80)}).observe(d.body,{subtree:true,childList:true,characterData:true});setInterval(apply,900)}if(d.readyState==='loading')d.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();