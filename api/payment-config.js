export default function handler(req,res){if(req.method!=='GET')return res.status(405).json({error:'GET 요청만 지원합니다.'});const clientKey=process.env.TOSS_CLIENT_KEY||'',amount=Number(process.env.HWADAM_REPORT_PRICE||0);res.status(200).json({enabled:!!clientKey&&Number.isFinite(amount)&&amount>0,clientKey,amount:Number.isFinite(amount)?amount:0,currency:'KRW',orderName:'화담 AI 사주상담 리포트'})}