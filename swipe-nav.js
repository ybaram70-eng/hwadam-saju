(()=>{const frame=document.getElementById('app');if(!frame)return;const order=['home','input','analysis','calendar','journal'];function activeIndex(){const b=document.querySelector('.navItem.active');const t=b?.dataset?.target;const i=order.indexOf(t);return i<0?0:i}function go(delta){const i=activeIndex(),n=Math.max(0,Math.min(order.length-1,i+delta));if(n===i)return;document.querySelector(`.navItem[data-target="${order[n]}"]`)?.click()}function bind(){let doc;try{doc=frame.contentDocument}catch{return}if(!doc||doc.documentElement.dataset.hwadamSwipeBound)return;doc.documentElement.dataset.hwadamSwipeBound='1';let sx=0,sy=0,started=false;doc.addEventListener('touchstart',e=>{if(e.touches.length!==1)return;const t=e.target;if(t?.closest?.('input,select,textarea,button,.luck,.years,[data-no-swipe]')){started=false;return}sx=e.touches[0].clientX;sy=e.touches[0].clientY;started=true},{passive:true});doc.addEventListener('touchend',e=>{if(!started||!e.changedTouches?.length)return;started=false;const dx=e.changedTouches[0].clientX-sx,dy=e.changedTouches[0].clientY-sy;if(Math.abs(dx)<70||Math.abs(dx)<Math.abs(dy)*1.35)return;if(dx<0)go(1);else go(-1)},{passive:true})}frame.addEventListener('load',()=>setTimeout(bind,120));setTimeout(bind,300)})();