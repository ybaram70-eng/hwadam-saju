import crypto from 'node:crypto';
function safeEq(a,b){const x=Buffer.from(a),y=Buffer.from(b);return x.length===y.length&&crypto.timingSafeEqual(x,y)}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({ok:false,error:'POST 요청만 지원합니다.'});const secret=process.env.TOSS_SECRET_KEY||'',expected=Number(process.env.HWADAM_REPORT_PRICE||0);if(!secret||!expected)return res.status(503).json({ok:false,error:'결제 서버 설정이 아직 완료되지 않았습니다.'});try{const token=String(req.body?.token||''),reportId=String(req.body?.reportId||'');const [body,sig]=token.split('.');if(!body||!sig||!reportId)return res.status(400).json({ok:false,error:'리포트 이용권 정보가 없습니다.'});const expectedSig=crypto.createHmac('sha256',secret).update(body).digest('base64url');if(!safeEq(sig,expectedSig))return res.status(403).json({ok:false,error:'유효하지 않은 리포트 이용권입니다.'});const payload=JSON.parse(Buffer.from(body,'base64url').toString('utf8'));if(payload?.v!==1||payload?.reportId!==reportId||Number(payload?.amount)!==expected)return res.status(403).json({ok:false,error:'이 리포트에 사용할 수 없는 이용권입니다.'});return res.status(200).json({ok:true,reportId:payload.reportId,orderId:payload.orderId,approvedAt:payload.approvedAt})}catch(e){return res.status(400).json({ok:false,error:'리포트 이용권을 확인할 수 없습니다.'})}}